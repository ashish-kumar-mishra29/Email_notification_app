import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class AuthService {

  constructor() { }
  // loginsendmai(){
  //   emailjs.init('fGa105Uwf8SbctAxG')
  //   let response = await emailjs.send("service_glc490q","template_gdsgddf44di156",{
  //     to_name: this.FormData.value.to_name,
  //     to_email: this.FormData.value.to_email,
  //     });}
  // }
//  loginemail(to_name:string):void{
//   let response = await emailjs.
//   ("service_glc490q","template_gdsgddf44di156",{
//         to_name: to_name
//         // to_email: this.FormData.value.to_email,
//       });
//  }
//  resend(to_name:string):void{
//   let response = await emailjs.send("service_glc490q","template_gdsgddf44di156",{
//         to_name: to_name
//         // to_email: this.FormData.value.to_email,
//       });
//  }
}
